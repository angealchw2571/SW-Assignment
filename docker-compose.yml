version: '2'
services:

  mycustomapp:
    container_name: mycustomapp
    restart: always
    image: 2569dad25f04
    environment:
      MYSQL_PW: "${MYSQL_PW}"
      SESSION_SECRET: "${SESSION_SECRET}"
      JWT_SECRET: "${JWT_SECRET}"
    ports:
      - "3002:3001"